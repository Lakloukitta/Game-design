// Test stub for the `server-only` marker package. In production this package
// throws if imported into a client bundle; in the Node test environment it is a
// harmless no-op so server modules can be unit/integration tested.
export {};
